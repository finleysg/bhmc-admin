# Prevent ESLint 9 from being hoisted so apps/public can use ESLint 8
public-hoist-pattern[]=!eslint
# Prevent zod from being hoisted so apps/public can use zod 3 while apps/api uses zod 4
public-hoist-pattern[]=!zod
