{
	"name": "monorepo",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"description": "BHMC Admin monorepo (scaffold)",
	"main": "index.js",
	"scripts": {
		"dev": "turbo run dev --parallel",
		"build": "turbo run build",
		"docker:up": "docker-compose up -d",
		"docker:down": "docker-compose down",
		"db:reset": "echo \"db:reset not implemented\"",
		"lint": "eslint --config packages/eslint-config/index.js .",
		"lint:fix": "eslint --config packages/eslint-config/index.js . --fix",
		"format": "prettier --write .",
		"format:check": "prettier --check .",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"packageManager": "pnpm@10.20.0",
	"devDependencies": {
		"@types/node": "^24.9.2",
		"eslint": "^9.18.0",
		"prettier": "^3.4.2",
		"turbo": "^2.5.8"
	},
	"dependencies": {
		"typescript": "^5.9.3"
	}
}
